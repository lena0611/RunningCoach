//
//  PaceLabWatchApp.swift
//  PaceLabWatch Watch App — PaceLAB watchOS 레이싱 앱 (#552)
//
//  Phase 0b: 워치 App 타겟 스켈레톤. 빌드 + RaceCore 링크 검증이 목적.
//  이후 Phase 1(HKWorkoutSession 라이브지표) → 2(고스트격차·햅틱) → 3(WCSession 왕복) → 4(상시표시·마감·음성).
//

import SwiftUI

@main
struct PaceLabWatch_Watch_AppApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
